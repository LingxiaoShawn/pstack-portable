interface:
  display_name: pstack · principle-boundary-discipline
  short_description: 'Place validation where trust, ownership or state guarantees
    change; keep domain logic separate from '
policy:
  allow_implicit_invocation: false
