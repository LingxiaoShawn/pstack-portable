interface:
  display_name: pstack · show-me-your-work
  short_description: 'Keep a reviewable decision trail for long-running or unattended
    work: a TSV log with one row per dec'
policy:
  allow_implicit_invocation: false
