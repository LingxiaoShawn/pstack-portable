interface:
  display_name: pstack · principle-sequence-verifiable-units
  short_description: Apply to multi-step work (sweeps, migrations, runs of similar
    edits) and to how you stack commits an
policy:
  allow_implicit_invocation: false
