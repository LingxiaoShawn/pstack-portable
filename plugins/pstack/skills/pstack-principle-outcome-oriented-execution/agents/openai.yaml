interface:
  display_name: pstack · principle-outcome-oriented-execution
  short_description: 'Apply during planned rewrites and migrations with explicit phase
    boundaries. Converge on the target '
policy:
  allow_implicit_invocation: false
