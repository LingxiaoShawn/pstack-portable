interface:
  display_name: pstack · principle-never-block-on-the-human
  short_description: 'Apply when tempted to ask ''should I do X?'' on reversible work.
    Proceed, present the result, let the '
policy:
  allow_implicit_invocation: false
