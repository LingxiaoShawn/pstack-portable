interface:
  display_name: pstack · principle-model-the-domain
  short_description: 'Apply when writing stateful logic, or when code branches a lot
    or repeats a shape assumption across '
policy:
  allow_implicit_invocation: false
