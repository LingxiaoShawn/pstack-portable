interface:
  display_name: pstack · principle-prove-it-works
  short_description: Apply after completing a task, before declaring done. Verify
    against the real artifact (run the feat
policy:
  allow_implicit_invocation: false
