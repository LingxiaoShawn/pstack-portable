interface:
  display_name: pstack · principle-attack-the-premise
  short_description: Apply when two or more fixes that share one premise have failed
    the same gate. Take a census of whic
policy:
  allow_implicit_invocation: false
