interface:
  display_name: pstack · principle-laziness-protocol
  short_description: Apply when refactoring, evaluating diff size, or tempted to add
    abstractions, layers, or signal thre
policy:
  allow_implicit_invocation: false
