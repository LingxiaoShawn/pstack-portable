interface:
  display_name: pstack · create-verification-skill
  short_description: Generate a project-local verification skill that drives your
    app the way a user does — any language,
policy:
  allow_implicit_invocation: false
