interface:
  display_name: pstack · principle-guard-the-context-window
  short_description: 'Apply when context is filling up: large outputs, long files,
    repeated reads, fan-out planning. Route'
policy:
  allow_implicit_invocation: false
