interface:
  display_name: pstack · principle-make-operations-idempotent
  short_description: Apply when designing commands, lifecycle steps, or processing
    loops that run amid crashes, restarts,
policy:
  allow_implicit_invocation: false
