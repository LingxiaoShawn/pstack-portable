### Session pickup
Read the supplied checkpoint or available current-project transcript. Use git,
the relevant PRs and actual files to verify its claims. Record what's completed,
in flight, blocked, or no longer true.

Do not guess another client's transcript directories. Without a transcript,
use visible context and the checkpoint and state the coverage limit.
Do not reuse an agent identifier unless the host confirms that worker still
exists. Resume from the first unmet acceptance criterion.
