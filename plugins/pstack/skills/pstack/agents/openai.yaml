interface:
  display_name: pstack · poteto-mode
  short_description: Use pstack for deliberate implementation, refactoring, investigation,
    and clear evidence-backed hand
policy:
  allow_implicit_invocation: false
