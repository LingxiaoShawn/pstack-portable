interface:
  display_name: pstack · tdd
  short_description: Use only when the user explicitly asks for TDD, a failing test,
    or a regression test, OR when the bu
policy:
  allow_implicit_invocation: false
