interface:
  display_name: pstack · principle-subtract-before-you-add
  short_description: Apply when sequencing an addition, refactor, or rewrite. Remove
    dead code, redundant validators, and
policy:
  allow_implicit_invocation: false
