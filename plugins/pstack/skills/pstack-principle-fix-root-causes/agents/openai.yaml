interface:
  display_name: pstack · principle-fix-root-causes
  short_description: Trace a defect to its mechanism and fix the violated contract
    while retaining necessary validation a
policy:
  allow_implicit_invocation: false
